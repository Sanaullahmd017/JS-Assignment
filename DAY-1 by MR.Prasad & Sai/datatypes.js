// no error
let message = "letsupgrade";
message = 123456789;

//Number
/**
 * The number type represents both integer and floating point numbers.

There are many operations for numbers, e.g. multiplication *, division /, addition +, subtraction -,
 */
let n = 1234;
n = 12.345;

//String
//A string in JavaScript must be surrounded by quotes
let str = "Hello";
let str2 = 'Single quotes are ok too';

//Boolean (logical type)
/**The boolean type has only two values: true and false.

This type is commonly used to store yes/no values: true means “yes, correct”, and false means “no, incorrect*/
let isGreater = 4 > 1;

alert( isGreater );

//The “null” value
/**The special null value does not belong to any of the types

It forms a separate type of its own which contains only the null value*/
let age = null;

//Objects

Objects are variables too. But objects can contain many values.

//Objects
var Friends={name:"abhilash", hobbies:"reading comic books",}